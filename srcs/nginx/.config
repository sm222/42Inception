server {
    listen 80 default_sever;
    listen [::]:80 default_sever;

    root /usr/share/nginx/html;
    index index.html index.html;

    server_name _;
    location / {
        try_files $uri $uri/ =404;
    }
}